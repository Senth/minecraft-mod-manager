{
  "name": "@mcman/gui",
  "description": "Library for minecraft-mod-manager",
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "electron": "latest",
    "electron-reload": "^2.0.0-alpha.1"
  },
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint -c .eslintrc --ext .ts ./src",
    "start": "yarn run build && electron ./dist/main.js"
  }
}
